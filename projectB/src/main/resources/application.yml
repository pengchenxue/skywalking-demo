eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8762
spring:
  application:
    name: projectB

debug: false
logging:
    config: classpath:log4j2-dev.xml
